machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t circle-murmur .
    
test:
  override:
    - docker run -d -p 64738:64738 circle-murmur; sleep 10
    - telnet 127.0.0.1 64738
